;;; init-config.el --- 其他配置 -*- lexical-binding: t -*-

;;; Commentary:
;; 杂项配置（原 Org 配置已移至 init-org.el）

;;; Code:

;; 可在此添加其他不属于 Org 的配置
;; 例如：项目特定设置、个人工作流等

(provide 'init-config)
;;; init-config.el ends here
